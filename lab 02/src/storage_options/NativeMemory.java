package storage_options;

/**
 * Native memory is used by the JVM for its internal operations, such as thread management and native libraries.
 * Examples for native memory are not typically written in Java, as this memory is managed by the JVM and is not directly
 * accessible from Java code. Native memory usage depends on the JVM implementation and operating system.
 */
public class NativeMemory {
}
